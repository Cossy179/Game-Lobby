// ai/answerChecker.js
function checkAnswer(teacherAnswer, studentAnswer) {
    // Placeholder function, replace with actual answer checking logic
    return teacherAnswer.toLowerCase() === studentAnswer.toLowerCase() ? 10 : 0;
}

module.exports = checkAnswer;
